---
import bibliography from '../content/json/bibliography.json';
const { bibliographyName } = Astro.props;
// const picked = (( `{${bibliographyName}}` ) => ( `{${bibliographyName}}` ))(bibliography);
// const specific = {${bibliography}.${bibliographyName}}
// console.log(`${bibliography.{$bibliographyName}}`)
---

<p>testo {`${bibliography}.${bibliographyName}`}</p>

<ul class="bibliography">
     <!-- {`${specific}`.map(({author, title, publisher, yearOfPublication, pages, ISBN, add, originalEditionYear})=>(<li><span>{originalEditionYear}</span> {author} <strong>"{title}"</strong>;<br />
    <span>{publisher} {yearOfPublication}, stron {pages}{ISBN ? `, ISBN: ${ISBN}` : null};<br />{add}</span></li>))} -->
</ul>
